# 6E-8 Preregistration

Cases recorded before implementation; see _offline/phase6e8/preregistered-cases.json.
Only existing runtime index.html may change, through a strictly opt-in synthetic
bootstrap. No app.js/config/theme/training changes. New files under coach-review-demo
and _offline/phase6e8. All earlier frozen sources preserved.
Synthetic example calories/loads/sleep/progress rules are explicit test fixtures,
not real-member norms or expert-approved coaching rules. Zero external AI costs.
Route B has member review + confirmation + separate apply, no trainer authorization.
Route A never falls back to B when its trainer is absent/revoked.
