# Package 6E-3 Offline

TECHNICAL PASS / READY FOR OWNER REVIEW. Private Node-only synthetic package.
No live adapter, provider, database, storage, automatic action or medical clearance.
Frozen phase6e0/phase6e1/phase6e2 are imported read-only.

Run from the repository root:
```text
node _offline/phase6e3/test/verify.cjs
node _offline/phase6e3/test/examples.cjs
```

prepare(state, nowMs, options, previousContext) is the frozen 6E-2 context issuer.
reflect({synthetic_only:true, locale, binding, sources}, context, authority)
validates the historical goal/session/catalog/set bundle and returns independent
access, facts, missing-source reasons, context feedback and nonphysical reflection.
validateSources is also executable separately. All source examples use syn- IDs.

The immutable historical snapshot must explicitly bind the goal at workout start.
A current goal/plan cannot replace missing history. Input versions and times are
synthetic declarations, not live server attestations. Partial sets or ambiguous
units keep facts only. No inferred trainer bounds, physical progression, nutrition
or recovery prescription. RIR/RPE remain separate self-reports, null is not zero.

76 preregistered cases precede the engine. Additional contract/isolation tests and
frozen regressions are separate from the known language limitation observation.
Exact contracts and owner proposals: docs/PHASE6E3_CONTRACTS.md and
docs/PHASE6E3_OWNER_OVERVIEW.md. No automatic acceptance/freeze or next package.
